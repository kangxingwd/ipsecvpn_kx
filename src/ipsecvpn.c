#include <rte_mbuf.h>
#include "ipsecvpn.h"

int is_vpn_dp_pkt(struct rte_mbuf *pkt) {
    if (pkt) {
        return 0;
    }
    return 0;
}

int ipsecvpn_process(struct rte_mbuf *pkt) {
    if (pkt) {
        return 0;
    }
    return 0;
}
